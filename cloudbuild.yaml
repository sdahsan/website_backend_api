steps:
  # 1. Build the backend container image
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build', 
      '-t', 'me-central1-docker.pkg.dev/$PROJECT_ID/website-chatbot-repos/website-backend-api:$COMMIT_SHA', 
      '.'
    ]

  # 2. Push it to your Artifact Registry warehouse
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'push', 
      'me-central1-docker.pkg.dev/$PROJECT_ID/website-chatbot-repos/website-backend-api:$COMMIT_SHA'
    ]

  # 3. Deploy it live to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args: [
      'run', 'deploy', 'website-backend-api',
      '--image', 'me-central1-docker.pkg.dev/$PROJECT_ID/website-chatbot-repos/website-backend-api:$COMMIT_SHA',
      '--region', 'me-central1',
      '--min-instances', '0',
      '--max-instances', '1',
      '--allow-unauthenticated'
    ]

images:
  - 'me-central1-docker.pkg.dev/$PROJECT_ID/website-chatbot-repos/website-backend-api:$COMMIT_SHA'

options:
  logging: CLOUD_LOGGING_ONLY
